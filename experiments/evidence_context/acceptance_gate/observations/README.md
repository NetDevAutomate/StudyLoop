# Readable synthetic observations

Model wording is preserved. Snapshot digests are consistently aliased to SnapshotNN,
and fingerprint values are omitted. Exact frozen originals remain in the private
run directory. The live code fingerprints were verified against the executed files.
These are authored synthetic cases, not real conversations or actual benchmarks.
`answer` fields are unverified model drafts; only `gate.released_answer` is the
policy-rendered result. Policy conformance does not verify source authenticity.
