# Stage 5 goal — finish the semantic retrieval layer (2026-09-12)

Owner directive (2026-09-12 17:13 BST): complete the remaining work with comprehensive testing
for each change, iterate on test results until everything passes with no issue, and put the plan,
each implementation and its test results to a council of models — GPT Astra (`openai.gpt-6-astra`),
Grok 4.6 (`grok-4.6`) and Kimi K2 (`kimi-k2-thinking`) — before acting on their findings.
Every BLOCKING/MAJOR is verified at source before it changes anything; receipts are immutable
(corrections as addenda); every number comes from a committed artefact.

State at the start: Stages 1–4 closed (`02bacc62`, CI green; `ea9fd0d3` docs). The live database
is at schema 48 with zero vectors (incident addendum, `stage4-record-2026-09-12.md`); the
production pin `fb606468` is schema 47 and refuses it, so **exports have failed silently since
20:13 BST on 2026-09-11**. Hybrid retrieval is implemented, measured, and off by default.

## Phase 0 — remediation (first, today)

| # | change | test that must pass |
|---|---|---|
| 0.1 | `~/.config/studyloop/config.yaml`: `semantic_search: {model: bge-small-en-v1.5, hybrid: false}` | `get_semantic_config()["model"] == "bge-small-en-v1.5"` from the pinned interpreter after 0.2 |
| 0.2 | production pin from CI-green `02bacc62`: worktree → `uv build --package agent-session-tools --wheel` → fresh-venv smoke → `uv tool install --force "<whl>[all]"` → `provenance.md` | pinned `CURRENT_VERSION == 48`; all nine console scripts `--help` exit 0; `studyloop` not importable from the tool venv; `~/.local/bin/session-query search <word> -n 1` exits 0 against the live database |
| 0.3 | recover the missing exports: `~/.local/bin/session-export` for each harness the hooks name | `MAX(messages.timestamp)` advances past 2026-09-11T19:13Z; session and message counts grow, never shrink; `user_version` stays 48; auto-embed writes only bge rows (model pin) |
| 0.4 | live backfill: `~/.local/bin/session-maint embed --model bge-small-en-v1.5 --batch-size 128` | `session-maint embed-check`: missing 0 / orphaned 0 / stale 0 / model_mismatch 0 / hidden 0; `~/.config/studyloop/sessions.vec.db` exists and rows = table rows; `studyloop doctor` `embeddings_alignment` passes; `STUDYLOOP_RETRIEVAL_MODE=hybrid session-query search … --output-format json` reports `mode: hybrid` |
| 0.5 | hook hygiene: every SessionEnd hook names the pinned absolute path and logs a failure instead of swallowing it (`~/.kiro/agents/study-mentor.json`, `~/.claude/settings.json`, and the in-repo templates they come from) | template unit test; a deliberately broken invocation leaves a line in `~/.config/studyloop/export-hook.log` and does not block the hook |
| 0.6 | `studyloop doctor` check `exporter_schema`: the exporter the hooks call must support the database's `user_version` (this incident was invisible for 21 h) | unit tests: pass when equal, fail with the fix text when the pin is older, pass when newer |

Finish line: exports flowing (0.3), alignment all-zero (0.4), doctor green (0.6), receipt
`stage5-remediation-receipt.json` with every number above.

## Phase 1 — SEALED, once (owner present)

`python -m agent_session_tools.eval gold --db ~/.config/studyloop/sessions.db --arms mcp,hybrid
--gold <owner's sealed file> --out …/stage4-sealed-bge-gold.json` at `73ee7221` or later. Read
G1 `hybrid_vs_mcp.ci95[0] >= 0.05` and G2 `hybrid_vs_mcp_K.upper_at_least_zero`. Record the
owner's R3 decision: keep the measured default (hybrid off, "semantic deferred") or write a **new**
latency pre-registration (gate, warm-up, load, repetitions, estimator, aggregation first) and
measure it — three back-to-back receipts on the live database.

## Phase 2 — close-out (three seats)

| # | change | test that must pass |
|---|---|---|
| 2.1 | ADR-0012 "Semantic retrieval: status as measured" in `docs/architecture/decisions/` | every number cites a receipt; the ADR index lists it |
| 2.2 | docs drift: `docs/session-memory.md`, `docs/cli-reference.md`, `docs/troubleshooting.md`, agent shared docs — hybrid off by default, how to enable, `session-maint embed`/`embed-check`, the doctor check | `just docs` build check passes; `grep` finds no claim that semantic search is on by default |
| 2.3 | openspec retirement: archive `openspec/changes/sessionweaver-phase2-retrofit` with specs synced | `openspec validate` clean; the change is under `openspec/changes/archive/` |
| 2.4 | sign-off: full suites of both packages, CI green on the final SHA, receipts index, plan-record closure addendum naming every stage's commits, owner hand-off note | `pytest` counts reported (the four sandbox-only dotenv tests named); CI link |
| 2.5 | cleanup: scratch clones reclaimed; `~/.config/studyloop` holds ~5 GB of old backups — list with sizes for the owner's decision, delete nothing without it | listing in the hand-off note |

## Council cadence

1. This plan — three seats, before Phase 0.2 installs anything.
2. Phase 0 implementation + test results + remediation receipt — three seats.
3. Phase 2 close-out (ADR, docs, sign-off) — three seats.
Iterate on each until no BLOCKING or MAJOR finding stands unaddressed; every finding's check is
recorded with its disposition in the stage record.
